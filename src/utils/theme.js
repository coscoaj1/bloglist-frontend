import { createTheme } from '@material-ui/core';

const theme = createTheme({
	palette: {
		primary: {
			main: '#b7b7a4',
		},
		secondary: {
			main: '#14213d',
		},
		common: {
			drawer: '#b7b7a4',
		},
	},
});

export default theme;
